# ============================================
# Vercel Veil - Environment Variables Example
# ============================================
# Copy this file to .env.local for local development
# Or configure these variables in your deployment platform (e.g., Vercel)

# ============================================
# Authentication (Required)
# ============================================
# Admin username for login
ACCESS_USERNAME=admin

# Admin password for login
ACCESS_PASSWORD=your-secure-password

# Optional: TOTP secret for 2FA. When set, 2FA is enabled.
# ACCESS_2FA_SECRET=your-totp-secret

# ============================================
# JWT Configuration (Required)
# ============================================
# JWT secret for token signing (minimum 32 characters recommended)
JWT_SECRET=your-jwt-secret-key-minimum-32-characters-long

# JWT expiration time (default: "1d")
# Format: number (seconds) or string with unit (e.g., "1d", "7d", "30d")
JWT_EXPIRES_IN=1d

# ============================================
# API Authentication (Required)
# ============================================
# API username for Basic Authentication
API_USERNAME=api_user

# API password for Basic Authentication
API_PASSWORD=secure_password

# Optional: API Token for Header Token authentication
# Used by Sonarr/Radarr webhooks (recommended)
# API_SECRET=super-secret-token

# Optional: API Token header name (default: "x-vv-token")
# API_TOKEN_HEADER=x-vv-token

# ============================================
# Email Notification (Resend) - Required for email features
# ============================================
# Resend API Key
RESEND_API_KEY=re_xxxxxxxxxxxxx

# Email sender address (must match Resend verified domain)
NOTIFICATION_EMAIL_FROM=Veil <noreply@example.com>

# Default recipient email address (supports comma-separated multiple addresses)
NOTIFICATION_EMAIL_TO=ops@example.com,alerts@example.com

# ============================================
# TMDB Configuration (Optional)
# ============================================
# TMDB API Key for movie metadata
# TMDB_API_KEY=tmdb_xxxxx

# TMDB query language (default: "zh-CN")
# TMDB_LANGUAGE=zh-CN

# TMDB query region (affects result sorting)
# TMDB_REGION=CN

# TMDB Session ID for user authentication (long-term valid)
# Required for favorite movies feature
# Get it from: https://www.themoviedb.org/settings/api
# TMDB_SESSION_ID=your-tmdb-session-id

# ============================================
# Douban RSS Configuration (Optional)
# ============================================
# Douban RSS URL for syncing movie list to TMDB favorites
# Used by /api/cron/sync/douban-to-tmdb cron job
# Example: https://www.douban.com/feed/people/148049852/interests
# DOUBAN_RSS_URL=https://www.douban.com/feed/people/148049852/interests

# ============================================
# TheTVDB Configuration (Optional)
# ============================================
# TheTVDB API Key for TV series metadata
# THE_TVDB_API_KEY=tvdb_xxxxx

# TheTVDB query language (default: "zh-CN")
# THE_TVDB_LANGUAGE=zh-CN

# ============================================
# Metadata Language Preference (Optional)
# ============================================
# Preferred metadata language when both TMDB and TheTVDB are available
# PREFERRED_METADATA_LANGUAGE=zh-CN

# ============================================
# GitHub GIST Storage (Optional)
# ============================================
# Used for movie list caching to reduce API requests
# GIST_ID=xxxxxxxxxxxxxxxxxxxx

# GitHub Personal Access Token (requires "gist" permission)
# Get it from: GitHub Settings > Developer settings > Personal access tokens
# GIST_TOKEN=ghp_xxxxxxxxxxxxxxxxxx

# ============================================
# Vercel Configuration (Optional)
# ============================================
# Vercel API access token (used to list vercel-* projects)
# VERCEL_ACCESS_TOKEN=your-vercel-access-token

# Comma-separated list of Vercel projects to exclude from display
# VERCEL_PROJECT_EXCLUDES=project1,project2

# ============================================
# Cron Jobs Configuration
# ============================================
# Cron job secret for authorization (Bearer token)
# If set, cron jobs can use Authorization: Bearer ${CRON_SECRET} header
# If not set, cron jobs will fallback to webhook authentication (API_SECRET + Basic Auth)
# CRON_SECRET=your-cron-secret
#
# Note: Cron job schedules should be configured in your external cron service (e.g., GitHub Actions)

# ============================================
# Application Base URL (Optional)
# ============================================
# Custom base URL for the application
# Used for generating share links and absolute URLs
# Can be full URL (https://example.com) or domain only (example.com)
# If not set, uses request origin or VERCEL_URL
# APP_BASE_URL=https://example.com

# ============================================
# Navidrome Music Server (Optional)
# ============================================
# Navidrome server URL
# NAVIDROME_URL=https://music.example.com

# Navidrome username for API authentication
# NAVIDROME_USERNAME=admin

# Navidrome password for API authentication
# NAVIDROME_PASSWORD=password

# ============================================
# Cache Configuration (Optional)
# ============================================
# Enable/disable cache (default: "1" for enabled, "0" for disabled)
# CACHE=1

# ============================================
# Build Time (Auto-generated)
# ============================================
# Build timestamp (automatically set during build, do not modify manually)
# NEXT_PUBLIC_BUILD_TIME=
