<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Prowlarr Notification</title>
    <style>
      body {
        margin: 0;
        padding: 32px 0;
        background-color: #e5e7eb;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      }
      .outer {
        max-width: 720px;
        margin: 0 auto;
        padding: 0 16px;
      }
      .card {
        background: #ffffff;
        border-radius: 18px;
        padding: 32px;
        border: 1px solid rgba(15, 23, 42, 0.08);
        box-shadow: 0 25px 60px rgba(15, 23, 42, 0.08);
      }
      .heading {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 16px;
      }
      .badge {
        background: #7c3aed;
        color: #fff;
        padding: 6px 14px;
        border-radius: 999px;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.08em;
      }
      .title {
        font-size: 28px;
        font-weight: 700;
        color: #111827;
        margin: 0;
        line-height: 1.2;
      }
      .subtitle {
        font-size: 16px;
        color: #6b7280;
        margin: 8px 0 0;
      }
      .info-grid {
        display: flex;
        gap: 16px;
        flex-wrap: wrap;
        margin: 24px 0;
      }
      .info-item {
        flex: 1 1 180px;
        background: #f4f6fb;
        border-radius: 12px;
        padding: 14px;
      }
      .info-item span {
        display: block;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: #6b7280;
      }
      .info-item strong {
        display: block;
        margin-top: 6px;
        font-size: 15px;
        color: #111827;
      }
      .status-badge {
        display: inline-flex;
        align-items: center;
        padding: 4px 12px;
        border-radius: 6px;
        font-size: 13px;
        font-weight: 600;
      }
      .status-healthy {
        background: #d1fae5;
        color: #065f46;
      }
      .status-unhealthy {
        background: #fee2e2;
        color: #991b1b;
      }
      .status-change {
        background: #fef3c7;
        color: #92400e;
      }
      .status-na {
        background: #f3f4f6;
        color: #6b7280;
      }
      .section {
        margin-top: 28px;
        padding: 20px;
        border-radius: 14px;
        background: #f8fafc;
      }
      .section h2 {
        margin: 0 0 14px;
        font-size: 16px;
        font-weight: 600;
        color: #111827;
      }
      .stack {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }
      .stack-item {
        padding: 12px 14px;
        border-radius: 10px;
        border: 1px solid rgba(15, 23, 42, 0.08);
        background: #fff;
        font-size: 14px;
        color: #0f172a;
        line-height: 1.6;
      }
      .stack-item strong {
        color: #374151;
        font-weight: 600;
      }
      .message {
        margin: 24px 0;
        padding: 16px;
        background: #fef3c7;
        border-left: 4px solid #f59e0b;
        border-radius: 8px;
        font-size: 14px;
        color: #92400e;
        line-height: 1.6;
      }
      .footer {
        margin-top: 32px;
        padding-top: 24px;
        border-top: 1px solid rgba(17, 24, 39, 0.08);
        font-size: 12px;
        color: #9ca3af;
        text-align: center;
      }
      .footer a {
        color: #7c3aed;
        text-decoration: none;
        font-weight: 500;
      }
      .footer a:hover {
        text-decoration: underline;
      }
      @media (max-width: 600px) {
        .card {
          padding: 24px;
        }
        .info-item {
          flex: 1 1 100%;
        }
        .heading {
          flex-direction: column;
          align-items: flex-start;
        }
      }
    </style>
  </head>
  <body>
    <div class="outer">
      <div class="card">
        <div class="heading">
          <div>
            <p style="margin: 0; text-transform: uppercase; letter-spacing: 0.2em; color: #a78bfa; font-size: 12px">Prowlarr</p>
            <h1 style="margin: 4px 0; font-size: 24px; color: #111827">{{indexerName}}</h1>
            <p class="subtitle">{{actionLabel}}</p>
          </div>
          <span class="badge">{{eventType}}</span>
        </div>

        <div class="info-grid">
          <div class="info-item">
            <span>Indexer</span>
            <strong>{{indexerName}}</strong>
          </div>
          <div class="info-item">
            <span>Protocol</span>
            <strong>{{protocol}}</strong>
          </div>
          <div class="info-item">
            <span>Instance</span>
            <strong>{{instanceName}}</strong>
          </div>
          <div class="info-item">
            <span>Status</span>
            <strong>{{statusChange}}</strong>
          </div>
        </div>

        <div class="section">
          <h2>Release Information</h2>
          <div class="stack">{{releaseDetails}}</div>
        </div>

        <div class="section">
          <h2>Indexer Configuration</h2>
          <div class="stack">{{indexerDetails}}</div>
        </div>

        <div class="message">{{message}}</div>

        <div class="footer">
          <p style="margin: 0; color: #9ca3af">This email was automatically generated by vercel-veil. Please do not reply directly.</p>
        </div>
      </div>
    </div>
  </body>
</html>

