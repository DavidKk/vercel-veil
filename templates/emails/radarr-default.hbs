<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Radarr Notification</title>
    <style>
      body {
        margin: 0;
        padding: 32px 0;
        background-color: #e5e7eb;
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
      }
      .outer {
        max-width: 720px;
        margin: 0 auto;
        padding: 0 16px;
      }
      .card {
        background: #ffffff;
        border-radius: 18px;
        padding: 32px;
        border: 1px solid rgba(15, 23, 42, 0.08);
        box-shadow: 0 25px 60px rgba(15, 23, 42, 0.08);
      }
      .heading {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 16px;
      }
      .badge {
        background: #be123c;
        color: #fff;
        padding: 6px 14px;
        border-radius: 999px;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.08em;
      }
      .hero {
        margin: 24px 0;
      }
      .hero-image {
        width: 100%;
        border-radius: 18px;
        overflow: hidden;
        border: 1px solid rgba(17, 24, 39, 0.08);
        box-shadow: inset 0 0 30px rgba(15, 23, 42, 0.1);
      }
      .hero-image img {
        width: 100%;
        height: auto;
        display: block;
      }
      .hero-content {
        margin-top: 20px;
      }
      .info-grid {
        display: flex;
        gap: 16px;
        flex-wrap: wrap;
      }
      .info-item {
        flex: 1 1 180px;
        background: #f4f6fb;
        border-radius: 12px;
        padding: 14px;
      }
      .info-item span {
        display: block;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.08em;
        color: #6b7280;
      }
      .info-item strong {
        display: block;
        margin-top: 6px;
        font-size: 15px;
        color: #111827;
      }
      .section {
        margin-top: 28px;
        padding: 20px;
        border-radius: 14px;
        background: #f4f6fb;
      }
      .stack {
        display: flex;
        flex-direction: column;
        gap: 10px;
      }
      .stack-item {
        padding: 12px 14px;
        border-radius: 10px;
        border: 1px solid rgba(15, 23, 42, 0.08);
        background: #fff;
        font-size: 14px;
        color: #0f172a;
      }
      .section h2 {
        margin: 0 0 14px;
        font-size: 16px;
        color: #111827;
      }
      .footer {
        margin-top: 32px;
        text-align: center;
        font-size: 12px;
        color: #94a3b8;
      }
      @media (max-width: 600px) {
        .card {
          padding: 24px;
        }
        .info-item {
          flex: 1 1 100%;
        }
      }
    </style>
  </head>
  <body>
    <div class="outer">
      <div class="card">
        <div class="heading">
          <div>
            <p style="margin: 0; text-transform: uppercase; letter-spacing: 0.2em; color: #f43f5e; font-size: 12px">Radarr</p>
            <h1 style="margin: 4px 0; font-size: 24px; color: #111827">{{movieTitle}}</h1>
          </div>
          <span class="badge">{{actionLabel}}</span>
        </div>

        <div class="hero">
          <div class="hero-image">
            <img src="{{{coverImage}}}" alt="poster" />
          </div>
          <div class="hero-content">
            <p style="margin: 0 0 8px; color: #475569; line-height: 1.7; font-size: 14px">{{synopsis}}</p>
            <p style="margin: 0">
              <a href="{{{detailUrl}}}" target="_blank" rel="noopener noreferrer" style="font-size: 13px; color: #0284c7; text-decoration: none"> View on TMDB → </a>
            </p>
          </div>
        </div>

        <div class="info-grid">
          <div class="info-item">
            <span>Event Type</span>
            <strong>{{eventType}}</strong>
          </div>
          <div class="info-item">
            <span>Year</span>
            <strong>{{year}}</strong>
          </div>
          <div class="info-item">
            <span>Instance</span>
            <strong>{{instanceName}}</strong>
          </div>
          <div class="info-item">
            <span>Download Client</span>
            <strong>{{downloadClient}}</strong>
          </div>
          <div class="info-item">
            <span>Is Upgrade</span>
            <strong>{{isUpgrade}}</strong>
          </div>
          <div class="info-item">
            <span>Action</span>
            <strong>{{actionLabel}}</strong>
          </div>
        </div>

        <div class="section">
          <h2>Release Information</h2>
          <div class="stack">{{releaseDetails}}</div>
        </div>

        <div class="footer">This email was automatically generated by vercel-veil. Please do not reply directly.</div>
      </div>
    </div>
  </body>
</html>

